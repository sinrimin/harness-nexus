# Generated by pnpm in its own style; `pnpm install` rewrites it, so formatting
# it here only produces churn. Everything else prettier sees is covered by
# .gitignore (which prettier 3 honours), so build output needs no entry.
pnpm-lock.yaml